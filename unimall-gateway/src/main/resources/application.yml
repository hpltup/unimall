server:
  port: 10011

spring:
  application:
    name: unimall-gateway
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
    config:
      name: gateway
      profile: dev
  config:
    import: configserver:http://127.0.0.1:10010
