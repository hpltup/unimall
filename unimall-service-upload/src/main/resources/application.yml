server:
  port: 10018

spring:
  application:
    name: unimall-service-upload
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
    config:
      name: upload
      profile: dev
  config:
    import: configserver:http://127.0.0.1:10010
