server:
  port: 10013

spring:
  application:
    name: unimall-service-goods
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
    config:
      name: goods
      profile: dev
  config:
    import: configserver:http://127.0.0.1:10010
