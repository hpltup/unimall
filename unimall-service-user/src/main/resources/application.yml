server:
  port: 10012

spring:
  application:
    name: unimall-service-user
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
    config:
      name: user
      profile: dev
  config:
    import: configserver:http://127.0.0.1:10010
