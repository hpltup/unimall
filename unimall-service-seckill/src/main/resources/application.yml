server:
  port: 10016

spring:
  application:
    name: unimall-service-seckill
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
    config:
      name: seckill
      profile: dev
  config:
    import: configserver:http://127.0.0.1:10010
