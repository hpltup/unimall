server:
  port: 10020

spring:
  application:
    name: unimall-service-search
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
    config:
      name: search
      profile: dev
  config:
    import: configserver:http://127.0.0.1:10010
