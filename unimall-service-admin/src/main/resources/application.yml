server:
  port: 10021

spring:
  application:
    name: unimall-service-admin
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
    config:
      name: admin
      profile: dev
  config:
    import: configserver:http://127.0.0.1:10010
